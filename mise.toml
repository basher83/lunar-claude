# Mise configuration for lunar-claude
min_version = "2024.9.5"

[settings]
auto_install = true
not_found_auto_install = true
task_run_auto_install = true

[settings.status]
show_tools = true
show_env = false
truncate = true

[plugins]
fnox-env = "https://github.com/jdx/mise-env-fnox"

# Pin concrete semvers — never "latest". The renovate preset
# (local>basher83/renovate-config // presets/mise.json) auto-merges mise tool
# bumps, but only for semver-pinned entries; "latest" is invisible to it, so a
# "latest" pin silently strands a tool at whatever was installed first (this is
# how hk froze at 1.36.0 past the 1.40.0 cache-roundtrip fix). Semver pins get
# the same currency as "latest" plus renovate visibility and rollback.
[tools]
python = "3.14.6"
fd = "10.4.2"
rg = "15.2.0"
git-cliff = "2.13.1"
shellcheck = "0.11.0"
uv = "0.11.32"
ruff = "0.16.0"
# "npm:pyright" = "1.1.407"  # Commented out due to npm registry timeout
lychee = "0.24.2"
"pipx:rumdl" = "0.2.42"
fnox = "1.31.1"
age = "1.3.1"
hk = "1.53.0"
pkl = "0.32.1"

[env]
_.python.venv = { path = ".venv", create = true } # create the venv if it doesn't exist
_.fnox-env = { tools = true }

[hooks]
postinstall = "hk install --mise && uv sync"

# === Claude Code CLI ===
[tasks.claude]
description = "Run Claude Code CLI with arguments"
run = "claude --dangerously-skip-permissions"
alias = "ccd"

[tasks.claude-continue]
description = "Run Claude Code CLI with arguments"
run = "claude --dangerously-skip-permissions --continue"
alias = "ccdc"

[tasks.hooks-install]
description = "Install hk and infisical hooks"
run = "hk install --mise && infisical scan install --pre-commit-hook"

# === Infisical tasks ===
[tasks.infisical-scan]
description = "Scan repository for secrets and sensitive data"
run = "infisical scan"

[tasks.markdown-lint]
description = "Lint Markdown files"
run = "rumdl check ."

[tasks.markdown-fix]
description = "Fix Markdown files"
run = "rumdl fmt ."

[tasks.intelligent-lint]
description = "Run intelligent markdown linting with AI agents"
run = "./scripts/intelligent-markdown-lint.py"

[tasks.intelligent-lint-dry-run]
description = "Analyze markdown linting errors (no fixes)"
run = "./scripts/intelligent-markdown-lint.py --dry-run"

[tasks.link-check]
description = "Check all links in markdown files using lychee"
run = "lychee --config lychee.toml '**/*.md'"

[tasks.link-check-verbose]
description = "Check all links in markdown files with verbose output"
run = "lychee --config lychee.toml --verbose debug '**/*.md'"

[tasks.validate]
description = "Validate marketplace structure"
run = "uv run scripts/verify-structure.py --strict"

[tasks.ci]
depends = ["lint", "test", "validate"]
description = "Full CI gate"
run = "echo 'CI passed'"

[tasks.lint]
description = "Run all checks"
run = "hk check"

[tasks.fix]
description = "Auto-fix issues"
run = "hk fix"

[tasks.sync]
description = "Install dependencies"
run = "uv sync"

[tasks.upgrade]
description = "Upgrade dependencies"
run = "uv lock --upgrade"

[tasks.test]
description = "Run tests"
run = "uv run pytest"

[tasks.typecheck]
description = "Type check"
run = "uv run pyright"

[tasks.format]
description = "Format code"
run = "ruff format ."

[tasks.changelog]
description = "Update changelog"
run = "git-cliff -o CHANGELOG.md"
