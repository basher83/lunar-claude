{
  "description": "Ansible workflows pipeline state management - blocks session stop when pipeline is active",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "uv run ${CLAUDE_PLUGIN_ROOT}/hooks/check-pipeline-state.py",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
